name: Inputs Demo
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Choose ENV to deploy
        required: true
        default: dev
jobs:
    inputs-demo:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy App
          run: echo "Deploying to ${{ inputs.environment }}"
